# Screenshot Placeholder

Replace this file with an actual screenshot of the ModelSet landing page.

## Recommended dimensions

- **Hero screenshot**: 1280×800px (16:10)
- **Format**: PNG (lossless) or WebP (compressed)

## What to capture

- The full Hero section showing the headline, terminal, and CTA buttons
- Clean browser window (no URL bar if possible)
- Light mode, desktop view (1280px width)

## How to capture

1. Open `index.html` in a browser at 1280px width
2. Use your OS screenshot tool or browser dev tools
3. Crop to the Hero section
4. Save as `screenshot-hero.png` in the `assets/` directory
